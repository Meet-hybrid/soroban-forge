# Writing Your First Contract

Use the templates in the `templates/` directory as a starting point.

```bash
cp -r templates/contract my-contract-name
```

Then add it to `Cargo.toml` workspace members.
